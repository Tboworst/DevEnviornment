[build-system]
requires = ["setuptools"]
build-backend = "setuptools.backends.legacy:build"

[project]
name = "create-app"
version = "0.1.0"
dependencies = ["click", "InquirerPy"]

[project.scripts]
create-app = "main:create_app"
